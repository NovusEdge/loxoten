#include<stdlib.h>
#include<stdio.h>
#include<time.h>

#include "corruptor.h"
#include "errors.h"

unsigned char *generate_random_bytes (size_t num_bytes);

int main(int argc, char const *argv[]) {

    return 0;
}
